apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.vaultIssuerNode.service.name }}
  labels: 
    {{ include "trustid-issuer.vaultIssuerNode.staticLabel" . }}
    {{- include "trustid-issuer.vaultIssuerNode.common.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "trustid-issuer.vaultIssuerNode.Labels" . | nindent 4 }}
    {{- include "trustid-issuer.vaultIssuerNode.selectorLabels" . | nindent 4 }}
  {{- with .Values.vaultIssuerNode.service.ports }}
  ports: 
    {{-  toYaml . | nindent 2 }}
  {{- end }}